const numbers = [1, 30, 49, 20, 29, 10, 13];
const found = numbers.find(number => number > 13);
const foundIndex = numbers.findIndex(numbers => numbers === 30);
console.log(found)
console.log(foundIndex)